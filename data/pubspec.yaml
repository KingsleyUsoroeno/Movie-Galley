name: data
description: A new Flutter project.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.7.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3

  domain:
    path: ../domain

dev_dependencies:
  flutter_test:
    sdk: flutter
